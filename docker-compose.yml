version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - ALLOWED_ORIGIN=http://localhost:5173
      - TRUST_PROXY=true

  frontend:
    build: ./frontend
    ports:
      - "5173:80"